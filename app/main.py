"""Application entrypoint placeholder for DevFlow Agent."""


def create_app() -> dict[str, str]:
    """Return minimal application metadata for the baseline skeleton."""

    return {
        "name": "devflow-agent",
        "stage": "iteration-1-baseline",
    }
