services:
  devflow-agent:
    image: python:3.11-slim
    working_dir: /workspace
    command: ["python", "-m", "app.main"]
    volumes:
      - ./:/workspace
    env_file:
      - .env.example

  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
